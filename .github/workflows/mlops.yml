name: CI Pipeline MLOps

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Instalar dependências
        run: pip install -r requirements.txt 

      - name: Executar notebooks Jupyter
        run: |
          jupyter nbconvert --to notebook --execute notebooks/01_exploracao.ipynb --output notebooks/01_exploracao_output.ipynb
          jupyter nbconvert --to notebook --execute notebooks/02_pipeline_validacao_pandas.ipynb --output notebooks/02_pipeline_validacao_pandas_output.ipynb
          jupyter nbconvert --to notebook --execute notebooks/04_monitoramento_fairness.ipynb --output notebooks/04_monitoramento_fairness_output.ipynb

      - name: Rodar testes automatizados
        run: pytest test_pipeline.py
